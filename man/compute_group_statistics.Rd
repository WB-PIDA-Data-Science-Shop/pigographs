% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/khmanalytics.R
\name{compute_group_statistics}
\alias{compute_group_statistics}
\title{Compute Min / Average / Max statistics for a given dimension,
grouped by an arbitrary variable (e.g. region, income level).}
\usage{
compute_group_statistics(data, dimension, grouping_var = region)
}
\arguments{
\item{data}{A data frame with at least country_name, the
dimension column, and the grouping column.}

\item{dimension}{Unquoted column name of the indicator
you want to summarise (e.g. pfm_inst).}

\item{grouping_var}{Unquoted column name to group by
(default = region, but you can pass income_level, etc.).}
}
\value{
A tidy data frame with one row per country–type combination,
where type ∈ {Min, Average, Max}.
}
\description{
Compute Min / Average / Max statistics for a given dimension,
grouped by an arbitrary variable (e.g. region, income level).
}
